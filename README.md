# Night Owl Real Estate Pricing Tool
Decision and support tool for home listing price recommendations.

## Setup
pip install -r requirements.txt

Baseline MAE = .5296 or roughly $52,957 on the test set with a test_size of 33%.
MAE representing the average absolute prediction error in dollars.