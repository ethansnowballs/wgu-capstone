# Night Owl Real Estate Pricing Tool
Decision and support tool for home listing price recommendations.

## Setup
pip install -r requirements.txt

Baseline MAE = .5332 or roughly $53,320 on the test set with a test_size of 20%.
Random Forest MAE = .3266 or roughly $32,661 on the test set with a test_size of 20%.